# Scripts

- `check-contracts.ps1` (planned): enforce contract/doc/golden update rule when schemas change.
- Add other helper scripts (e.g., smoke runner) here; ensure they are documented and cross-platform where possible.
