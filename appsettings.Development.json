{
  "AppConfig": {
    "Adapters": {
      "WorkItemsAdapter": "fake", // Default to fake adapter for safety - change to "github" or "jira" for live operations
      "VcsAdapter": "fake" // Default to fake adapter for safety - change to "git" for live operations
    },
    "LivePolicy": {
      "PushEnabled": false, // Default to false for safety - require explicit opt-in for push operations
      "DryRun": true, // Default to true for safety - require explicit opt-in for live operations
      "RequireCredentialsValidation": true // Whether to validate credentials before allowing live adapters
    },
    "SemanticKernel": {
      "DefaultProvider": "openai",
      "DefaultModel": "gpt-4",
      "MaxTokens": 2048,
      "Temperature": 0.3,
      "Timeout": "00:02:00"
    }
  }
}